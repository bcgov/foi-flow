import React from 'react';
import "./admin.scss";

const Admin = React.memo(() => {
    return (
        <div className="admin-page">
            <p>Test Admin</p>
        </div>
    );
});

export default Admin;
